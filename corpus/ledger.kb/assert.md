---
claim: ASSERT
text: All non-stipulated claims are asserted: proven exactly to the extent their requirements are proven
---

Ported so `LEAST_FIX`'s premise arrow resolves (`premisesResolve`). The
design ledger holds this claim as `certified(review 089)`; that evidence
record awaits richer verb support in the corpus port.
