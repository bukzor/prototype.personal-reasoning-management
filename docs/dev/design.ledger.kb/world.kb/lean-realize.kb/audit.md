# Audit spoke — external re-checking

lean4export + lean4checker re-check the corpus under an external kernel in CI:
`AUDIT_SPOKE`, the one hedge against `TRUST_COST` available once `TRUST_SWAP`
is forfeit under `NATIVE!`. An archival and audit channel, not a workflow
component.
