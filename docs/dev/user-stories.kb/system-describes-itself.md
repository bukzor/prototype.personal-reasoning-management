# The system describes itself

The design ledger that specified this system — stance, ledger, conduct,
convention — becomes its first corpus. Porting it shakes out the surface
ergonomics before any DTPL content arrives, and afterward the system's
own design commitments are claims the system checks.

**Served by:** SELF_APPLY, SELF_HOST!. Already build-order step 6 in the
design sketch; it is also the acceptance test for
`shallow-corpus-still-pays.md`, since the design ledger is almost
entirely stated-depth claims.
