# From-homebrew dev-environment dependencies (linuxbrew under crostini).
# The Lean toolchain itself is NOT brew's job: elan reads ./lean-toolchain
# and installs the pinned Lean+Lake per-repo.
brew "elan-init"
